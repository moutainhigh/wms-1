server:
  port: 8080
  address: localhost
logging:
  level:
    org:
       springframework:
           security: DEBUG
    root: INFO
spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=yyt
    username: sa
    password: zhupeng123
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.teeny.wms.web.repository
security:
  basic:
    enabled: false
